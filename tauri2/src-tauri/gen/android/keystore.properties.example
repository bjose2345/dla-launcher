# Copy this file to keystore.properties. The destination is ignored by Git.
# Prefer an absolute storeFile path so local and CI builds resolve the same key.
storeFile=/absolute/path/to/dla-launcher-release.jks
storePassword=replace-with-a-secret
keyAlias=release
keyPassword=replace-with-a-secret
